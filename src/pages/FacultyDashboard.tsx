import { useState } from "react"

export default function FacultyDashboard(){
    const[students,setStudents]=useState([]);
    return(<div className="text-black">
        <h1>Re-registered Students</h1><br />

        </div>)
}